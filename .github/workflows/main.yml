name: update

on:
  schedule:
     - cron:  '0 21 * * 2,6'

jobs:
  update_timetable:
    runs-on: ubuntu-latest
    steps:
    - name: curl
      run: |
        curl -X GET ${{ secrets.URL_FOR_UPDATE }}
